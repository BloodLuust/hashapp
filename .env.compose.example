# Minimal env file for Docker Compose (dotenv format)
# Copy to .env.compose and fill your real values.

# JWT secret for backend auth
JWT_SECRET=change-me-please

# MongoDB connection
MONGO_URI=mongodb://localhost:27017/test
MONGO_DB=hashapp

# Optional: Frontend client WS base (for browser to reach backend)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

